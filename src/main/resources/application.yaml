

server:
  port: 8080
  servlet:
    context-path: /api-irctc

spring:
  application:
    name: IRCTC-APP
  datasource:
    url: jdbc:mysql://localhost:3306/irctc-app-dev
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
